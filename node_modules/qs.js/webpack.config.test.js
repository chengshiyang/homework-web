module.exports = {
    entry: [
      "webpack-dev-server/client?http://localhost:4333",
      "webpack/hot/dev-server",
      'mocha!./test/test.js'
    ],
    output: {
        path: __dirname,
        filename: 'test.js'
    },
    module: {
        loaders: [{
            test: /\.jsx?$/,
            exclude: /(node_modules|bower_components)/,
            loader: 'babel',
            query: {
                presets: ['es2015', "stage-0"]
            }
        }]
    },
    resolve: {
        extensions: ['', '.js', '.json', '.jsx']
    }
}
