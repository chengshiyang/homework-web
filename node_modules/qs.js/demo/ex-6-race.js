import qs from '../src/qs';
import $ from './query';


let btn = $('#btn-ex-6');
let btnClick = qs.fromEvents(btn, 'click');

btnClick.merge(qs.delay(5000, 'init')).take(1).onValue( val => {
    console.log('GO!');
});
