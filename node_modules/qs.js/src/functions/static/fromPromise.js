import Stream from '../../Stream';


export default function fromPromise(p) {
    return new Stream( emitter => {
        p.then( val => {
            emitter.emit(val);
            emitter.end();
        }, err => {
            emitter.error(err);
            emitter.end();
        })
    }).toProperty();
}
